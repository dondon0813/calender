# 品牌整合說明（把「品牌」分頁併進「團購品牌資料庫」）

## 這次做了什麼

前台食材／食譜頁的「品牌篩選圖磚」和「品牌介紹卡」，原本讀後台的 **`品牌`** 分頁。
現在改成統一由 **`團購品牌資料庫`** 這一個分頁控制，多加兩個欄位：

- **`顯示於食材食譜`**：填 `是` 的品牌，才會出現在前台食材/食譜的品牌篩選。空白＝不顯示。
- **`品牌介紹`**：前台食材頁選該品牌時，上方那張介紹卡的文字（可留空）。

之後你只要維護 `團購品牌資料庫` 一個分頁，`品牌` 分頁可以停用/刪除。

> ⚠️ 資安：前台是**免登入公開**讀取。程式只會把 `品牌名稱` 和 `品牌介紹` 兩欄吐給前台，
> `LINE窗口／Email窗口／IG窗口／品牌備註` 這些內部資料**不會外洩**。

---

## 你要做的事（照順序）

### 步驟 1：在「團購品牌資料庫」分頁補兩個欄位

打開後台試算表 → `團購品牌資料庫` 分頁，在**最右邊（現有「更新時間」欄後面）**新增兩欄，
標題請**完全照這樣打**（順序也要一樣）：

| … 更新時間 | 顯示於食材食譜 | 品牌介紹 |
|---|---|---|

- ⚠️ 一定要加在**最後面**，不要插在中間，否則既有欄位會錯位。

### 步驟 2：把舊「品牌」分頁的資料搬過來

對照舊 `品牌` 分頁，每個要出現在食材/食譜頁的品牌：

1. 在 `團購品牌資料庫` 找到（或新增）同名的那一列。
   - ⚠️ **品牌名稱要跟食材資料庫／成品資料庫的「品牌」欄字面完全一致**，前台才篩得到。
2. 該列 `顯示於食材食譜` 欄填 `是`。
3. 舊分頁該品牌有「品牌介紹」的話，把介紹文字貼到 `品牌介紹` 欄。

沒填 `是` 的品牌就不會出現在前台（等於舊分頁沒放的效果）。

### 步驟 3：更新 Apps Script（.gs）並重新部署

程式改動都已經在 repo 的 `Code.gs` 完整版裡（`getBrandDbSheet_`、`getBrandDbList_`、
`addBrandDb_`、`updateBrandDb_`、`doGet` 的 `publicBrands`、`doPost` 的 brand-db-add/update）。

1. 把 repo 最新的 `Code.gs` 內容整份貼進 Apps Script 編輯器（或只套用上述幾個函式的差異）。
2. 右上「部署」→「管理部署作業」→ 現有部署的鉛筆 → 版本選「新版本」→ 部署。
   - ⚠️ 一定要**編輯現有部署**，不要新增，網址才不會變。

### 步驟 4：驗收

1. 前台食材頁 / 食譜頁：品牌篩選圖磚 = 你在 `團購品牌資料庫` 填「是」的那些品牌。
2. 點某品牌 → 上方介紹卡文字 = `品牌介紹` 欄的內容（沒填就不出現介紹卡）。
3. 後台「團購品牌資料庫」新增/編輯品牌的視窗，多了「顯示於食材／食譜頁」勾選框和「品牌介紹」欄，
   存檔後前台會跟著變。
4. 全部正常後，舊的 `品牌` 分頁就可以刪掉了。

---

## 備註

- 後台編輯品牌的勾選框存到試算表時會寫成 `是`/空白，跟手動填的一致，兩種方式可混用。
- 前台程式（recipes.html 等）**完全沒改**，因為輸出格式（`品牌名稱`/`品牌介紹`）跟原本一樣。
