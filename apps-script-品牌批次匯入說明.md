# 品牌批次匯入＋圖片欄位＋多廠商　部署說明

## 這次改了什麼

1. `團購品牌資料庫` **D 欄改名**：`經典商品圖片網址` → **`去背小圖`**（同一件事，不新增欄位）
2. `團購品牌資料庫` 尾端**只加一欄**：**M `品牌圖片`**（大合照／介紹頁用）
3. `所屬廠商ID`（B 欄）**改成可填多家**，逗號分隔（例：`V001,V004`）；後台改成複選清單
4. `行事曆` 新增 **W `去背小圖`**（這檔專用，空白就自動用品牌預設）
5. 新增一次性函式 `importBrandsFromCalendar()`，把 59 個合作品牌灌進資料庫

---

## 步驟 1：試算表改欄位（**先做，不然匯入會錯位**）

### 後台試算表 →「團購品牌資料庫」分頁

- **D1** 標題從 `經典商品圖片網址` 改成 `去背小圖`（**只改標題文字，欄位不要刪**，原本的網址資料留著）
- **M1** 新增標題 `品牌圖片`

改完第 1 列應該是：

| A | B | C | D | E | F | G | H | I | J | K | L | M |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| id | 所屬廠商ID | 品牌名稱 | 去背小圖 | LINE窗口 | Email窗口 | IG窗口 | 品牌備註 | 建立時間 | 更新時間 | 顯示於食材食譜 | 品牌介紹 | 品牌圖片 |

### 行事曆試算表 →行事曆分頁

V 欄「對應品項」之後，**W1** 新增標題 `去背小圖`。

---

## 步驟 2：重新部署 .gs

把 repo 的 `Code.gs` 全文貼進 Apps Script 專案覆蓋，然後
**部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**。

⚠️ 不要按「新增部署作業」，那會產生新網址，前台就連不到了。

---

## 步驟 3：執行批次匯入

Apps Script 編輯器上方函式下拉選 **`importBrandsFromCalendar`** → 按 ▶ 執行。

- 以**品牌名稱**比對，已存在的自動跳過 → **重跑不會產生重複列**
- 執行完看「執行紀錄」：「新增 N 個品牌」「已存在略過 N 個」
- 新增的品牌 `顯示於食材食譜` 一律留空（不公開），要在食材／食譜頁露出的再自己勾

---

## 步驟 4：手動補的兩筆

1. **行事曆編號 33**（7/30「防偷拍定位器」）→ K 欄「品牌」填 `MOMAX`
2. **廠商資料庫**若還沒有 `樂奎`，先新增，再回品牌資料庫把 MOMAX 的所屬廠商指過去
3. **精臣**：到後台品牌編輯視窗，所屬廠商**同時選那兩家**（按住 Ctrl 點選）

---

## 去背小圖／品牌圖片怎麼填

| 情境 | 填哪裡 |
|---|---|
| 這個品牌大部分團都用同一張小圖 | 品牌資料庫 **D 欄**（預設值），行事曆 W 欄留空 |
| 同品牌不同品項要用不同小圖（例：卡蘿琳益生菌 vs 軟糖Q凍） | 該檔的行事曆 **W 欄**各自填 |
| 品牌介紹頁的大圖／合照 | 品牌資料庫 **M 欄** |

前台之後的邏輯：`該檔W欄小圖 || 品牌D欄小圖`。

去背小圖建議**正方形、透明背景**的 PNG 或 WebP，排在開學清單上方才不會大小不一。
